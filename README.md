# Shiver reference assembly

## Installation/running
No prior installation is required for Nextflow workflows. 
- Usually Nextflow workflows hosted on Github can be run simply by passing a Github path (e.g. `nextflow run mattarnoldbio/`)